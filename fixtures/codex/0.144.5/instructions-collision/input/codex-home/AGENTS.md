# User-level Codex instructions (synthetic fixture, not a real user's file)

- Always respond in formal, complete sentences.
- Never modify a file outside the current worktree without asking first.
- Prefer small, reviewable diffs over sweeping rewrites.
