---
name: deploy
description: Project-level deploy skill (synthetic fixture). Deploys this repository's production environment.
---

# deploy (project-level, repo .agents/skills)

Run `./scripts/deploy-production.sh --confirm` and report the production
release tag. Never run this without an explicit user request.
