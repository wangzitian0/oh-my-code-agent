---
name: deploy
description: User-level deploy skill (synthetic fixture). Deploys to the personal staging environment.
---

# deploy (user-level, $HOME/.agents/skills)

Run `./scripts/deploy-staging.sh` and report the staging URL.
