# PR-06 fixture: Codex CLI 0.144.5, same-name ("deploy") Skill collision
# across three discovery roots (docs/knowledge/README.md §10 item 2).
apiVersion: omca.dev/v1alpha1
kind: FixtureInvocation
host: codex
surface: cli
version: 0.144.5
platform: darwin-arm64
cwd: project
env: {}

invoke:
  attempted: false
  reason: >-
    Same reasoning as instructions-collision/invocation.yaml: no safe
    non-interactive flag dumps Codex's resolved skill catalog (which of
    several same-named discovered skills would actually be offered/loaded)
    without a real, model-invoking session. `codex --help` was checked and
    exposes no such introspection flag. Recorded UNKNOWN rather than
    guessed.

binary:
  acquisitionMethod: >-
    Installed as an asdf-managed nodejs (20.19.0) npm global package:
    `npm install -g @openai/codex`.
  resolvedPath: >-
    /Users/SP14016/.asdf/installs/nodejs/20.19.0/lib/node_modules/@openai/codex/node_modules/@openai/codex-darwin-arm64/vendor/aarch64-apple-darwin/bin/codex
  sha256: 5e29ab10ca1171be158f7335dd6bd8ce1aaf9af1556939db36a5ee338be6f5f2
  versionSource: >-
    `codex --version` -> "codex-cli 0.144.5"; see fixtures/README.md for the
    full acquisition/provenance writeup shared by every codex/* case.

observationRules:
  - root: home
    concept: skill
    scope: user
    surface: cli
  - root: project
    concept: skill
    scope: workspace
    surface: cli
  - root: codex-home
    concept: skill
    scope: user
    surface: cli
