# User-level Codex config (synthetic fixture, not a real user's config).
# $CODEX_HOME/config.toml

[mcp_servers.shared-tools]
command = "npx"
args = ["-y", "@example/shared-tools-server"]
env = { EXAMPLE_TOKEN = "user-level-placeholder-not-a-real-secret" }

[mcp_servers.user-only-server]
command = "npx"
args = ["-y", "@example/user-only-server"]
