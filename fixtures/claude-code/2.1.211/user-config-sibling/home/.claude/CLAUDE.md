# user memory (fixture)

Synthetic user-global instructions, present only to prove this fixture's
`home/.claude/` asset directory (the CLAUDE_CONFIG_DIR default) and
`home/.claude.json` (the sibling file this fixture exists to exercise) are
observed as the two distinct things they are — never merged into one.
