---
name: deploy
description: Personal (user-level) deploy skill (synthetic fixture). Deploys to the personal staging environment.
---

# deploy (personal, ~/.claude/skills)

Run `./scripts/deploy-staging.sh` and report the staging URL.
