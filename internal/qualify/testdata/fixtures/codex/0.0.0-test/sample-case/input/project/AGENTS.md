# project instructions (test fixture)

This is a synthetic project-level instruction file used only by
internal/qualify's own package tests. It deliberately conflicts with
input/home/AGENTS.md's content to exercise the same collision shape the real
fixtures use, without being part of the real fixtures/ corpus.
